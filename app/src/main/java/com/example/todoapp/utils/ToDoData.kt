package com.example.todoapp.utils

/**
 * Data class representing a to-do task with its unique task ID and task description.
 * @property taskId The unique identifier for the to-do task.
 * @property task The description of the to-do task.
 */
data class ToDoData(val taskId: String, val task: String)
